% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{rmorielite_fetch}
\alias{rmorielite_fetch}
\title{Polite, cache-aware HTTPS fetch}
\usage{
rmorielite_fetch(
  url,
  dest_basename = NULL,
  rate = 4,
  force = FALSE,
  max_tries = 3L
)
}
\arguments{
\item{url}{The URL to fetch.}

\item{dest_basename}{Filename (no path) under which to cache the
result. Defaults to the URL's basename.}

\item{rate}{Requests per second. Default \code{4}.}

\item{force}{If \code{TRUE}, re-download even if a cached copy exists.
Default \code{FALSE}.}

\item{max_tries}{Maximum retry attempts on transient errors
(429, 5xx). Default \code{3}.}
}
\value{
A character scalar: the absolute path of the cached file.
}
\description{
Downloads a single URL to the per-user cache directory (see
\code{\link[=rmorielite_cache_dir]{rmorielite_cache_dir()}}) using \code{httr2} with built-in throttling
and exponential-backoff retry on 429/5xx. If a non-empty cached
copy at the same target path already exists, the download is
skipped and the cached path is returned.
}
\details{
Throttling and retry are delegated to \code{\link[httr2:req_throttle]{httr2::req_throttle()}} and
\code{\link[httr2:req_retry]{httr2::req_retry()}}. The default rate cap of 4 requests per
second matches the polite-by-default conventions used by major
public-data CKAN portals.
}
\examples{
\donttest{
path <- rmorielite_fetch(
  "https://data.ontario.ca/api/3/action/package_list"
)
file.exists(path)
}

}
