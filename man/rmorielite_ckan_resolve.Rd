% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ckan_resolve.R
\name{rmorielite_ckan_resolve}
\alias{rmorielite_ckan_resolve}
\title{Resolve a CKAN resource by name pattern}
\usage{
rmorielite_ckan_resolve(portal, package_slug, name_pattern, timeout = 30)
}
\arguments{
\item{portal}{Base URL of the CKAN portal, without trailing slash.
Example: \code{"https://data.ontario.ca"}.}

\item{package_slug}{CKAN package slug. Example:
\code{"data-on-inmates-in-ontario"}.}

\item{name_pattern}{PCRE-compatible regex applied case-insensitively
to each resource's \code{name} field. The first match wins.}

\item{timeout}{Request timeout in seconds. Default \code{30}.}
}
\value{
A list with elements \code{name}, \code{url}, \code{format}, \code{resource_id},
and \code{package_slug}, or \code{NULL} if the package is not found, the API
call fails, or no resource matches.
}
\description{
Queries a CKAN open-data portal's \code{package_show} endpoint and
returns metadata for the first \emph{resource} whose \code{name} field
matches a supplied regular expression. CKAN powers many government
open-data portals (Ontario, UK, Canada, US federal); resource UUIDs
occasionally change between portal updates but resource names are
usually stable, so name-pattern matching survives portal reslugs.
}
\examples{
\donttest{
res <- rmorielite_ckan_resolve(
  portal        = "https://data.ontario.ca",
  package_slug  = "data-on-inmates-in-ontario",
  name_pattern  = "Restrictive.?Confinement.*Detailed.?Dataset"
)
if (!is.null(res)) res$url
}

}
